# v0.9.8 GitHub Actions 与 GHCR 自动发布检查清单

- [ ] CI workflow 可运行：查看 `.github/workflows/ci.yml` 的最近执行结果
- [ ] PR 构建可通过：发起到 `main` 的 PR 并确认 CI 通过
- [ ] tag `v0.9.8` 可触发 GHCR 发布：推送 tag 后检查 `.github/workflows/publish-ghcr.yml` 执行记录
- [ ] GHCR Packages 页面可看到镜像：`ghcr.io/wangjiqing/xingyu-music-vault`
- [ ] 镜像 tag 包含 `v0.9.8` / `v0.9` / `latest`
- [ ] 使用 image compose 可拉取镜像：`docker compose -f deploy/docker-compose.image.example.yml config`
- [ ] 拉取镜像后后台页面可访问：`curl -i http://localhost:8080/`
- [ ] OpenAPI 服务信息接口可访问：`curl -i http://localhost:8080/api/open/v1/server/info`
- [ ] healthcheck 正常：`curl -i http://localhost:8080/api/health`
- [ ] 镜像中不包含本地音乐文件：`docker run --rm ghcr.io/wangjiqing/xingyu-music-vault:v0.9.8 sh -lc 'ls -la /music'`
- [ ] 镜像中不包含 SQLite 数据库：`docker run --rm ghcr.io/wangjiqing/xingyu-music-vault:v0.9.8 sh -lc 'test ! -f /app/data/music-vault.db'`
- [ ] 镜像中不包含真实 token：`docker history ghcr.io/wangjiqing/xingyu-music-vault:v0.9.8`
- [ ] Docker Hub 仍为手动发布或后续版本处理：参考 `docs/release/image-publish.md`
