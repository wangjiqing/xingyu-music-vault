quarkus:
  http:
    host: 0.0.0.0
    port: 8080
    record-request-start-time: true
    access-log:
      enabled: true
      pattern: '%r %s %D ms'
  datasource:
    db-kind: sqlite
    jdbc:
      url: jdbc:sqlite:${MUSIC_VAULT_DB_PATH:/app/data/music-vault.db}
  hibernate-orm:
    database:
      generation: none
    dialect: org.hibernate.community.dialect.SQLiteDialect
  flyway:
    migrate-at-start: true
  swagger-ui:
    always-include: true
  smallrye-openapi:
    info-title: Xingyu Music Vault API
    info-version: 0.1.0
    info-description: Self-hosted music metadata management API.
  log:
    category:
      "com.xingyu.musicvault":
        level: INFO

music-vault:
  data-dir: ${MUSIC_VAULT_DATA_DIR:/app/data}
  config-dir: ${MUSIC_VAULT_CONFIG_DIR:/app/config}
  music-dirs: ${MUSIC_VAULT_MUSIC_DIRS:/Users/wangjiqing/Project/Musics/Music}
  lyric-dirs: ${MUSIC_VAULT_LYRIC_DIRS:/Users/wangjiqing/Project/Musics/Lyrics}
  db-path: ${MUSIC_VAULT_DB_PATH:/app/data/music-vault.db}
  api-token: ${MUSIC_VAULT_API_TOKEN:change-me}
  ffprobe-path: ${MUSIC_VAULT_FFPROBE_PATH:/usr/bin/ffprobe}
  ffmpeg-path: ${MUSIC_VAULT_FFMPEG_PATH:/usr/bin/ffmpeg}

music:
  scan:
    default-path: ${MUSIC_SCAN_DEFAULT_PATH:/Users/wangjiqing/Project/Musics}

"%dev":
  quarkus:
    datasource:
      jdbc:
        url: jdbc:sqlite:data/music-vault.db
    log:
      category:
        "com.xingyu.musicvault":
          level: DEBUG
  music-vault:
    data-dir: data
    config-dir: config
    music-dirs: /Users/wangjiqing/Project/Musics/Music
    lyric-dirs: /Users/wangjiqing/Project/Musics/Lyrics
    db-path: data/music-vault.db

"%test":
  quarkus:
    datasource:
      jdbc:
        url: jdbc:sqlite:target/test-music-vault.db
    flyway:
      clean-at-start: true
      clean-disabled: false
  music-vault:
    data-dir: target/test-data
    config-dir: target/test-config
    music-dirs: target/test-music
    lyric-dirs: target/test-music
    db-path: target/test-music-vault.db
